# Python dependencies for the Bangalore Premium Property Assistant.
# Install with:  pip install -r requirements.txt
# Then install the browser once:  python -m playwright install chromium
#
# Versions pinned to what this project was built and validated against.

playwright==1.62.0     # JS-capable scraping of builder/portal/RERA sites
rapidfuzz==3.14.5      # fuzzy promoter -> Tier-1 builder name matching
pypdf==6.14.2          # (optional) parse RERA certificate PDFs later
